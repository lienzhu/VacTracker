<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <stroke android:color="@color/colorBackgroundBlue"
                android:width="3dip"/>
            <gradient
                android:startColor="@color/colorBackgroundBlue"
                android:centerColor="@color/colorBackgroundBlue"
                android:centerY="1.0"
                android:endColor="@color/colorBackgroundBlue"
                android:angle="270"

                />
        </shape>

    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip"/>
                <stroke android:color="@android:color/transparent"
                    android:width="3dip"/>
                <gradient
                    android:startColor="#EF2D56"
                    android:centerColor="#EF9C57"
                    android:centerY="0.2"
                    android:endColor="#F7F976"
                    android:angle="45"
                    />
            </shape>
        </clip>
    </item>
</layer-list>